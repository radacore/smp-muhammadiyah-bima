# Root deployment env for docker-compose.vps.yml
# Copy to .env on the VPS:  cp .env.example .env  then edit values.
# docker compose otomatis baca file .env di working dir.

# Base URL final website sekolah, harus diakhiri "/".
# Wajib https:// kalau domain di belakang reverse proxy + SSL (NPM).
SEKOLAH_BASE_URL=https://smpmuhammadiyahbima.com/
